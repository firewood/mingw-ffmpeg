PACKAGE=zlib
ENABLED=yes
VERSION=1.2.5
ARCHIVETYPE=tar.gz
ARCHIVEFILE=${PACKAGE}-${VERSION}.${ARCHIVETYPE}
ARCIVEHOME=http://www.zlib.net/
DOWNLOADURI=http://zlib.net/${ARCHIVEFILE}
PATCH1=
SRCDIR=
PREBUILD1=
CLEAN="make -f win32/Makefile.gcc clean"
BUILD="make -f win32/Makefile.gcc"
POSTBUILD1="cp -a libz.a /mingw/lib/"
POSTBUILD2="cp -a zlib.h zconf.h /mingw/include/"
