PACKAGE=nasm
ENABLED=yes
VERSION=2.08.01
ARCHIVETYPE=tar.bz2
ARCHIVEFILE=${PACKAGE}-${VERSION}.${ARCHIVETYPE}
ARCIVEHOME=http://www.nasm.us/
DOWNLOADURI=http://www.nasm.us/pub/nasm/releasebuilds/${VERSION}/${ARCHIVEFILE}
PATCH1=
SRCDIR=
PREBUILD1="./configure --prefix=/mingw"
CLEAN="make clean"
BUILD="make"
POSTBUILD1="make install"
